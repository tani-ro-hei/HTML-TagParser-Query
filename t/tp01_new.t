# ----------------------------------------------------------------
    use strict;
    use Test::More tests => 2;
    BEGIN { use_ok('HTML::TagParser::Query') };
# ----------------------------------------------------------------
    my $html = HTML::TagParser::Query->new();
    ok( ref $html, "new instance" );
# ----------------------------------------------------------------
;1;
# ----------------------------------------------------------------
